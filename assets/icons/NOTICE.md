# Icons

Every icon the client draws comes from the console kit (`pf-console-ui` in
`unom/punktfunk`): the Lucide marks as path data, the launcher and OS marks as vector tables.
`LICENSE-lucide.txt` is Lucide's ISC licence; the launcher and OS marks' licences are in
`../cards/LICENSES/`.
