//! What is left of the app's own UI layer now that the console kit draws everything: the
//! geometry types (`render`), the animation clocks and the focus map the Home grid navigates
//! with, the modal fade.

pub mod animation;
pub mod fade;
pub mod focus;
pub mod render;
pub mod scroll;
pub mod widgets;
